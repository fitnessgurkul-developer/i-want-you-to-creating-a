# Git LFS disabled for this repo.
# GitHub LFS budget was exceeded, which broke Render/Netlify clones
# (smudge filter fails on paths like GALLERY/ABOUT US/RAVI MISKA.png).
# Production site media lives under assets/ as normal git files.

# Staff password for /backend.html
ADMIN_TOKEN=change-me-now
# ADMIN_PASSWORD also works as an alias

HOST=127.0.0.1
PORT=8000

# Cloud deploy (Render / Railway / Fly / Node host):
# HOST=0.0.0.0
# CORS_ORIGINS=https://fitnessgurukul.app,https://www.fitnessgurukul.app
# DATA_DIR=./data

# Optional AI chat (Node + Python backends)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6

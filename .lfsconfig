# Skip downloading Git LFS objects on CI/cloud clones (Render/Netlify/Vercel).
# Gallery media is not required for the Python API service.
# Pair with env GIT_LFS_SKIP_SMUDGE=1 on Render.
[lfs]
	fetchexclude = *
